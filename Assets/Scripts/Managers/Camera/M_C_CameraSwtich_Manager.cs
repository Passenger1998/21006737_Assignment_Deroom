using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class M_C_CameraSwtich_Manager : MonoBehaviour
{

    public Camera camera_front, camera_back, camera_left, camera_right;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
